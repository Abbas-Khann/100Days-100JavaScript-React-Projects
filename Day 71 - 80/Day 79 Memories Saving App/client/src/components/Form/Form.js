import React from "react";
import useStyles from "./styles";

const Form = () => {
    const styles = useStyles();

    return(
        <div>
            <h1>Form</h1>
        </div>
    )
}

export default Form